def TS_32():
    Log.Message("32. Verify that the new/in-progress/completed check indexing jobs are not accessible by any other admin/supervisor/processor/ClipStich/Provider users by editing the url values ", "")
    KeywordTests.Admin_login.Run()
    Browsers.Item[btChrome].Navigate(Project.Variables.URL + "/dashboard")
    Aliases.chrome.BrowserWindow.Maximize()
    Aliases.chrome.pageEdit3.textnodeUploadBatch.linkManualCheckIndexDataCapture.Click()
    Aliases.chrome.pageNew2.Wait()
    Project.Variables.job_id = DBTables.TS_32.Values[0,0] 
    Aliases.chrome.ToUrl(Project.Variables.URL + "/check_index/check_data_capture?id=" + Project.Variables.job_id + "&page=1")
    Aliases.browser.BrowserWindow.Maximize()
    aqObject.CheckProperty(Aliases.browser.pageRevRemit.panelFlashError, "contentText", cmpContains, "The Job is already allocated to another user", False)
    Aliases.chrome.pageEdit3.imageLogout.Click()
    Aliases.browser.pageRevRemit.Wait()
    Aliases.chrome.BrowserWindow.Close()
    Delay(1000)